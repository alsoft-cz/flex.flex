----------------------------------------------------------------------------------------------------
module public cc_env =
-- P©eklada‡.
-- #environment promˆnn‚.
----------------------------------------------------------------------------------------------------
-- Ondra : 22.02.2000 : Vytvo©il.
----------------------------------------------------------------------------------------------------

extend
  cc_lexsym;

--with
--  cc_lexsym;

---- %%LANG Form t data a ‡asu ---------------------------------------------------------------------
-- Datum se uv d¡ ve form tu ISO 8601, odstavec 5.2.1.1, Extended
-- format a to bez ohledu na to, zda se jedn  o UTC nebo o lok ln¡ ‡as
-- (co‘ je v rozporu s ISO 8601, odstavec 5.3.3).
--   €as se uv d¡ ve form tu ISO 8601, odstavec 5.3.1.1, Extended
-- format.
--
-- P©¡klady:
--    2000-23-02  -- datum 2. £nora 2000
--    13:23:59    -- ‡as 13 hodin 23 minut 59 sekund
----------------------------------------------------------------------------------------------------

type
  -- #environment promˆnn‚
  tenvvar      = enum
      env_none;                                  -- nezn m  promˆnn 
      ---- Jazykem definovan‚ #environment promˆnn‚ ----------------------
      -- £daje o jazyku
      env_language_version;                      -- verze jazyka
      -- £daje o p©eklada‡i
      env_compiler_name;                         -- jm‚no p©eklada‡e
      env_compiler_type;                         -- typ p©eklada‡e
      env_compiler_subtype;                      -- subtyp p©eklada‡e
      env_compiler_version;                      -- verze p©eklada‡e
      -- £daje o tomto p©ekladu
      env_compile_date;                          -- datum p©ekladu (UTC)
      env_compile_time;                          -- ‡as p©ekladu (UTC)
      env_compile_local_date;                    -- datum p©ekladu (lok ln¡)
      env_compile_local_time;                    -- ‡as p©ekladu (lok ln¡)
      -- parametry platformy na kter‚ bˆ‘¡ p©eklada‡
      env_host_os_name;                          -- viz env_target_*
      env_host_os_family;                        -- viz env_target_*
      env_host_os_type;                          -- viz env_target_*
      env_host_os_version;                       -- viz env_target_*
      env_host_os_subsystem_type;                -- viz env_target_*
      env_host_os_subsystem_version;             -- viz env_target_*
      env_host_machine_architecture;             -- viz env_target_*
      env_host_machine_version;                  -- viz env_target_*
      env_host_machine_bits;                     -- vic env_target_*
      env_host_machine_direct_byteorder;         -- viz env_target_*
      -- parametry c¡lov‚ platformy
      env_target_os_name;                        -- jm‚no c¡lov‚ho opera‡n¡ho syst‚mu
      env_target_os_family;                      -- skupina opera‡n¡ch syst‚mu (UNIX, WIN32)
      env_target_os_type;                        -- typ opera‡n¡ho syst‚mu (Linux/BSD, 95/NT)
      env_target_os_version;                     -- verze opera‡n¡ho syst‚mu
      env_target_os_subsystem_type;              -- typ subsyst‚mu (driver/GUI/console)
      env_target_os_subsystem_version;           -- verze subsyst‚mu
      env_target_machine_architecture;           -- architektura po‡¡ta‡e (IA-32/AS400)
      env_target_machine_version;                -- verze architektury (5-P1, 6-P2)
      env_target_machine_bits;                   -- ¨¡©ka slova v bitech (32, 64, 128)
      env_target_machine_direct_byteorder;       -- po©ad¡ byt–
      ---- Implementac¡ definovan‚ #environment promˆnn‚ -----------------
      end enum;

----------------------------------------------------------------------------------------------------
procedure env_search (
    id             : in tentityident;  -- identifik tor
    env            : out tenvvar);     -- odpov¡daj¡c¡ promˆnn 
-- P©evede identifik tor na #environment promˆnnou.
----------------------------------------------------------------------------------------------------
procedure env_get_var (
     env          : in tenvvar;        -- po‘adovan  promˆnn 
     lex          : out lexsym;        -- odpov¡daj¡c¡ lexik ln¡ symbol
     val          : out lexval);       -- a jeho hodnota
-- Hodnota zadan‚ #environment promˆnn‚.
----------------------------------------------------------------------------------------------------
procedure env_init_var;
-- Inicializace hodnot #environment promˆnn˜ch.
----------------------------------------------------------------------------------------------------

end cc_env;